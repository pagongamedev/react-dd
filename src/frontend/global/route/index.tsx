import p000Onboard from '../../domain/p000-onboard';
import { useContextUser } from '../context/contextUser';
const i18nList: any[] = [];

const JSX = (props: any) => {
  return <p000Onboard.JSX />;
};

export default { i18nList, JSX };
